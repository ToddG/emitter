{erl_opts, [no_debug_info]}.
{deps, []}.

{escript_incl_apps,
 [wavegen]}.
{escript_main_app, wavegen}.
{escript_name, wavegen}.
{escript_emu_args, "%%! +sbtu +A1\n"}.

%% Profiles
{profiles, [{test,
             [{erl_opts, [debug_info]}
            ]}]}.
